---
title: 先决条件
category: Intro to Predicates
parent:
  label: All Guides
  link: /guides
---

# 先决条件

## 安装

<TextImport
  file="../installation/index.mdx"
  comment="install_fuelup"
  commentType="{/*"
/>

<CodeImport
  file="../installation/index.mdx"
  comment="install_fuelup_command"
  commentType="{/*"
  lang="sh"
  trim="true"
/>

<TextImport
  file="../counter-dapp/building-a-smart-contract.mdx"
  comment="install_help"
  commentType="{/*"
/>

<TextImport
  file="../installation/index.mdx"
  comment="install_VSCode_extensions"
  commentType="{/*"
/>

### 已经安装了`fuelup`?

<TextImport
  file="../counter-dapp/building-a-smart-contract.mdx"
  comment="already_installed"
  commentType="{/*"
/>
## 项目设置

从一个新的空文件夹开始，将其命名为 `multisig-predicate`。


<TestAction
id="create-project-folder"
action={{
  name: 'runCommand',
  commandFolder: 'guides-testing'
}}
/>

```sh
mkdir multisig-predicate
```

进入 `multisig-predicate` 文件夹。

```sh
cd multisig-predicate
```

在终端中，首先创建一个名为 `predicate` 的新 Sway 项目：

<TestAction
id="create-predicate"
action={{
  name: 'runCommand',
  commandFolder: 'guides-testing/multisig-predicate'
}}
/>

```sh
forc new --predicate predicate
```

> 提示：注意 `--predicate` 标志，它告诉 `forc` 您要基于 **断言** 而不是默认的 **合约** 程序类型创建项目。

从 `forc` 命令生成的项目结构应该如下所示：

<TestAction
id="predicate-tree"
action={{
  name: 'runCommand',
  commandFolder: 'guides-testing/multisig-predicate'
}}
/>

```sh
tree predicate
```

```sh
predicate
├── Forc.toml
└── src
    └── main.sw

1 directory, 2 files
```

进入您的断言文件夹。

```sh
cd predicate
```

在 VSCode 中，导航到 `predicate` 文件夹中的 `src` 文件夹，在那里您将找到一个名为 `main.sw` 的文件。这是您的 Sway 断言将被编写的文件。